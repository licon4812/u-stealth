<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="UStealth.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UStealth.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tableView="using:WinUI.TableView"
    Title="U-Stealth"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="16" RowDefinitions="Auto,*">
        <Button
            x:Name="RefreshButton"
            Width="100"
            Height="32"
            HorizontalAlignment="Right"
            Click="RefreshButton_Click"
            Content="Refresh" />
        <tableView:TableView
            x:Name="DrivesTableView"
            Grid.Row="1"
            Margin="0,16,0,0"
            x:DataType="local:DriveInfoModel"
            AutoGenerateColumns="False"
            DoubleTapped="DrivesTableView_DoubleTapped"
            ItemsSource="{x:Bind ViewModel.Drives}"
            SelectionMode="Single">
            <tableView:TableView.Columns>
                <tableView:TableViewTextColumn
                    Width="62"
                    Binding="{Binding SystemDrive}"
                    Header="System Drive"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="55"
                    Binding="{Binding DriveLetter}"
                    Header="Drive Letter"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="80"
                    Binding="{Binding Interface}"
                    Header="Interface"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="200"
                    Binding="{Binding VolumeLabel}"
                    Header="Volume Label"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="200"
                    Binding="{Binding Format}"
                    Header="Format"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="200"
                    Binding="{Binding Model}"
                    Header="Model"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="123"
                    Binding="{Binding MediaType}"
                    Header="Media type"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="75"
                    Binding="{Binding Size}"
                    Header="Size"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="75"
                    Binding="{Binding Status}"
                    Header="Status"
                    IsReadOnly="True" />
                <tableView:TableViewTextColumn
                    Width="0"
                    Binding="{Binding DeviceID}"
                    Header="DeviceID"
                    IsReadOnly="True"
                    Visibility="Collapsed" />
            </tableView:TableView.Columns>
        </tableView:TableView>
    </Grid>
</Window>
